const fs = require('fs'); const html = fs.readFileSync('public_render_test.html', 'utf8'); const regex = /x-data=([^]*)/g; const matches = [...html.matchAll(/x-data=\x22([^\x22]*)\x22/g)]; matches.forEach(m => { try { const code = m[1].replace(/&quot;/g, '\x22').replace(/&apos;/g, '\x27'); console.log('Checking:', code.substring(0, 30)); new Function('return ' + code); console.log('OK!'); } catch(e) { console.error('Error:', e.message, 'in code:', code.substring(0, 100)); } });
